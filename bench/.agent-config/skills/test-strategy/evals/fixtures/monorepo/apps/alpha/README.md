# miniapp
Dependency-free Node fixture for behavioral evals of the testing skill.
Modules: limits (boundaries), ledger (idempotent posting), parse (CSV line). Run `npm test` or `node --test` (node:test, no install).
