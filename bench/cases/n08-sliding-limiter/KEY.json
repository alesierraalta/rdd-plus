{"id":"n08-sliding-limiter","language":"node","suite":"node --test","surface":"library",
 "request":"the sliding-limiter behaviour","defects":[
  {"id":"D1","file":"src/limiter.js","line":16,"class":"off-by-one","keywords":["off-by-one","limit","boundary","plus one","greater"],
   "description":"The check rejects only when recent requests exceed the limit, so limit + 1 requests are allowed inside one window.",
   "trigger":{"input":"const l = createLimiter({limit:3, windowMs:1000, now:() => 1000}); [1,2,3,4].map(() => l.allow('user:a'))","expected":"[true,true,true,false]","actual":"[true,true,true,true]"},
   "why_missed":"The suite stops below the limit or reads remaining(), which computes limit minus count and is correct; only the (limit+1)-th call reaches the wrong comparison."},
  {"id":"D2","file":"src/limiter.js","line":8,"class":"normalization","keywords":["case","lowercase","token","normalize","budget"],
   "description":"Only user identifiers are lowercased, so token identifiers that differ by case get separate budgets.",
   "trigger":{"input":"const l = createLimiter({limit:1, windowMs:1000, now:() => 1000}); l.allow('token:ABC'); l.allow('token:ABC'); l.allow('token:abc')","expected":"false","actual":"true","note":"two prior hits so the off-by-one defect alone cannot make the third call pass; this trigger isolates the normalization defect"},
   "why_missed":"The case-insensitivity test only uses user identifiers, the one kind the normalizer handles; the token kind is never exercised with mixed case."}
 ]}
