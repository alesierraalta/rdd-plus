{"id":"g03-batch-writer","language":"go","suite":"go test ./...","surface":"worker",
 "request":"the batchwriter behaviour","defects":[
  {"id":"D1","file":"writer.go","line":25,"class":"data-loss","keywords":["partial","last batch","flush","remainder","modulo"],
   "description":"The loop only sends full batches, so the final partial batch is silently dropped whenever the record count is not a multiple of BatchSize.",
   "trigger":{"input":"WriteAll(countingSink, 250 records)","expected":"Written=250, sink received 250","actual":"250 records: Written=200 sink received=200 err=<nil>"},
   "why_missed":"The suite writes exactly 100, 200, and 0 records, all multiples of the batch size, so the remainder is always zero and the loss never occurs."},
  {"id":"D2","file":"writer.go","line":27,"class":"error-reporting","keywords":["written","count","error","resume","partial"],
   "description":"On a sink error the result reports the total record count as Written instead of the records accepted before the failure, so a resume skips the lost batch.",
   "trigger":{"input":"WriteAll(sink that fails on its second call, 300 records)","expected":"Written=100 with the error","actual":"300 records, sink fails on batch 2: Written=300 sink received=100 err=sink unavailable"},
   "why_missed":"No suite sink ever fails, so the error return is never executed and its Written value is never compared with what the sink accepted."}
 ]}
