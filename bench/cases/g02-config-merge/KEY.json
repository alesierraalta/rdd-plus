{"id":"g02-config-merge","language":"go","suite":"go test ./...","surface":"library",
 "defects":[
  {"id":"D1","file":"config.go","line":20,"class":"config-merge","keywords":["zero value","false","disable","override","explicit"],
   "description":"Merge applies Debug only when the override sets it to true, so an explicit false is treated as unset and no higher layer can ever disable debug.",
   "trigger":{"input":"Load(Config{Debug: boolp(true)}, Config{Debug: boolp(false)}).Debug","expected":"false","actual":"Debug=true"},
   "why_missed":"Every suite override enables debug, so the guard that only honors true is never distinguished from honoring any set value."},
  {"id":"D2","file":"config.go","line":29,"class":"config-merge","keywords":["zero value","retries","0","disable","override","explicit"],
   "description":"Merge applies Retries only when the override sets a non-zero value, so an explicit 0 is treated as unset and retries cannot be turned off from a higher layer.",
   "trigger":{"input":"Load(Config{Retries: intp(5)}, Config{Retries: intp(0)}).Retries","expected":"0","actual":"Retries=5"},
   "why_missed":"The suite overrides retries to 5; zero is the one set value the guard drops, and no test asks for it."}
 ]}
