{"id":"n13-vacuous-assert","language":"node","suite":"node --test","surface":"library",
 "defects":[
  {"id":"D1","file":"src/clamp.js","line":4,"class":"boundary-clamp","keywords":["clamp","maximum","boundary","upper","return"],
   "description":"The above-maximum branch returns the unclamped value instead of max, so any value greater than max passes through unbounded.",
   "trigger":{"input":"clamp(15, 0, 10)","expected":"10","actual":"15"},
   "why_missed":"The only test named for the above-maximum case asserts that the result is a number, a property every numeric return satisfies, so the suite stays green whether or not the value was actually clamped; the boundary reads as covered by its test name."}
 ]}
