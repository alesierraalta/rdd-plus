bin/
*.bak
.runs/
results/
__pycache__/
.codegraph/
# tgrep's local index: the same class of machine-local state as .codegraph/, never part of a change.
.tgrep/
bench/results/
bench/**/ws/
# Local Pi runtime state
.atl/

# Throwaway Claude config the benchmark builds; holds a symlink to the operator credentials.
bench/.agent-config/

# Harness state the gentle-ai sync writes into a project
.pi/

# Local-only tests (no-excess-tests routing destination): never committed.
testLocales/
